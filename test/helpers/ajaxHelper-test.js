const assert = require('chai').assert
const sinon = require('sinon')
const ajaxHelper = require('../../lib/helpers/ajaxHelper')

describe('ajaxHelper calls', function() {
  context('get requests', function() {
    it('retrieve all foods', function() {

    })
  })
})
